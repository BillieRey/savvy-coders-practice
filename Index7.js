const function writePsycoBook(num){
  let counter = 0
  for (counter = num; num <=0;counter = ++)
        myString = (`${num} what the hell`)
        LngString = LngString + mystring }
     return LngString
     n=writePsycoBook(10)
        console.log(n)
