const numberSummerMachine = {
  startingNumber: 1,
  endingNumber: 5,
  summedNumbers: 0,
  ttlNumbers: 0,
  sumDaNumbers: function(){
     for(this.endingNumber;this.endingNumber >= this.startingNumber;this.ttlNumbers ++);               {this.summedNumbers + this.startingNumber};
        numberSummerMachine.sumDaNumbers();
console.log(`This is the total of the numbers{numberSummerMachine.this.summedNumbers}`);
