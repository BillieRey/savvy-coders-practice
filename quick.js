const numberSummerMachine = {
        startingNumber: 1,
        endingNumber: 5,
        summedNumbers: 0,
          sumDaNumbers: function(){
              for(let counter = this.endingNumber;counter <= this.startingNumber;counter ++);
                      {this.summedNumbers + this.startingNumber}
        }
      }; 
            numberSummerMachine.sumDaNumbers();
